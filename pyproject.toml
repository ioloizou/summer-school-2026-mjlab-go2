[build-system]
requires= ["uv_build>=0.8.18,<0.9.0"]
build-backend= "uv_build"

[project]
name = "unitree_go2_velocity"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = ["mjlab==1.4.0"]

[project.entry-points."mjlab.tasks"]
unitree_go2_velocity = "unitree_go2_velocity"